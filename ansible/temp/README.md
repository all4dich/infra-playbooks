# Ansible Example with `include_*` and `import_*` 

This directory contains temporary Ansible playbooks and related files. It is primarily used for:

- **Testing `import_*` and `include_*`:** 

**Contents:**

- `main.yml`: The starting point playbook for test
  - Usage: `ansible-playboo main.yml`

**Note:** The contents of this directory may change frequently and are not guaranteed to be stable or production-ready.
